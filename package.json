{
  "name": "api-debugger",
  "private": true,
  "version": "0.0.5",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "echo build not configured",
    "dev": "echo dev not configured"
  }
}
